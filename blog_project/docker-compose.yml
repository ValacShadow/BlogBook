# docker-compose.yml
version: '3.11'

services:
  web:
    build: .
    container_name: fastapi_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: "redis:latest"
    container_name: redis_cache
    ports:
      - "6379:6379"
